#!/usr/bin/python3
def text_indentation(text):
    if not isinstance(text, str):
        TypeError("text must be a string")
    for t in text:
        if t == ".":
            a
        if t == ":":
            a
        if t == "?":
            a
